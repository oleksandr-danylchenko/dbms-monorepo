{
  "name": "dbms-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "Fractional implementation of the DBMS system",
  "main": "index.js",
  "author": "Oleksandr Danylchenko",
  "license": "MIT",
  "packageManager": "yarn@3.1.0",
  "scripts": {
    "build": "npm_config_yes=true yarn workspaces foreach run build",
    "start": "npm_config_yes=true yarn workspaces foreach run start",
    "build-server": "cd packages/server && yarn build",
    "start-server": "cd packages/server && yarn start" ,
    "start-client": "cd packages/client && yarn start",
    "build-client": "cd packages/client && yarn build"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "devDependencies": {
    "@types/node": "^16.11.7",
    "prettier": "^2.4.1"
  }
}
